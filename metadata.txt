# This file contains metadata for your plugin. Since 
# version 2.0 of QGIS this is the proper way to supply 
# information about a plugin. The old method of 
# embedding metadata in __init__.py will 
# is no longer supported since version 2.0.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=Cloud Masking
description=CloudMasking is a Qgis plugin for make the masking of clouds, cloud shadow, cirrus, aerosols, ice/snow and water for Landsat (4, 5, 7 and 8) products using different process and filters such as Fmask, Blue Band, Cloud QA, Aerosol and Pixel QA.
about=CloudMasking is a Qgis plugin for make the masking of clouds, cloud shadow, cirrus, aerosols, ice/snow and water for Landsat (4, 5, 7 and 8) products using different process and filters such as Fmask, Blue Band, Cloud QA, Aerosol and Pixel QA.
version=18.4.23
qgisMinimumVersion=2.14
qgisMaximumVersion=2.99

author=Xavier Corredor Llano, SMBYC
email=xcorredorl@ideam.gov.co

homepage=https://smbyc.bitbucket.io/qgisplugins/cloudmasking
tracker=https://bitbucket.org/smbyc/qgisplugin-cloudmasking/issues
repository=https://bitbucket.org/smbyc/qgisplugin-cloudmasking

# Uncomment the following line and add your changelog:
changelog=
    18.4.23
    - Implemented the possibility to select multiple mask layers to apply
    - Clipping the stack in the trimmed extent when is applying with only shape area
    - Fix keep the data outside the shape area as valid data (=1), important for apply several mask
    - Fix marking the data (as 255 value) outside the shapefile or selected area if option is selected
    - Fix error clipping when switch masking between rectangular and shape region
    - Updated the Rios and Fmask libs
    - Fixes and improve the documentation
    - Fix applying the mask loaded externally unsetting the nodata value before process it
    - Enable parallel for some process in fmask only in Linux and Mac
    18.2.7
    - Fix get layer by name for layers no activated
    - Restore option for show not only cloud mask in select mask
    18.2.6
    - Fix the unicode for MTL file
    - Improved the remove temporal files and reload plugin
    - Adjusted the plugin icon and about dialog
    - Added about button for open the online documentation
    - Improved the icons/buttons for plugin info
    - Reordered icons and prefer use svg format
    - Fixes the horizontal policy for the combobox list and browse button
    - Refactoring and simplified the selector and load the shape file for made the mask in specific area using QgsMapLayerComboBox
    - Refactoring and simplified the combobox list for select the mask layer for apply using QgsMapLayerComboBox
    - Fixed unicode path string in all input/output file dialogs

# Tags are comma separated with spaces allowed
tags=cloud,masking,landsat,raster,remote sensing

category=Raster
icon=icons/cloud_masking.svg
# experimental flag
experimental=False

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False
