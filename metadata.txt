[general]
name=Cloud Masking
description=CloudMasking is a Qgis plugin for make the masking of clouds, cloud shadow, cirrus, aerosols, ice/snow and water for Landsat (4, 5, 7 and 8) products using different process and filters such as Fmask, Blue Band, Cloud QA, Aerosol and Pixel QA.
about=CloudMasking is a Qgis plugin for make the masking of clouds, cloud shadow, cirrus, aerosols, ice/snow and water for Landsat (4, 5, 7 and 8) products using different process and filters such as Fmask, Blue Band, Cloud QA, Aerosol and Pixel QA.

version=19.8.15
qgisMinimumVersion=3.0
qgisMaximumVersion=3.99

author=Xavier Corredor Llano, SMByC
email=xcorredorl@ideam.gov.co

homepage=https://smbyc.github.io/CloudMasking
tracker=https://github.com/SMByC/CloudMasking/issues
repository=https://github.com/SMByC/CloudMasking

changelog=
    19.8.15:
    - Change the rectangular extent selection to polygon AOI picker to generate the
      masking only inside the polygons drawn by user
    - Add the option to export the mask layers generated in one combined mask file
    - Add the option to use a scratch polygon layer for generate the mask inside it
    - Fix some error checking the inputs images in fmask and rios lib
    - Fixed the output masked when the plugin is applying the mask in particular
      file with nodata value set
    - Improve and fix the error handler and the wait process decorators
    - Improve the load layers function to fix load specific file types
    - Fix functions to get the file path of layer or files
    - Fix function to get the file path of layer with layername string
    - Improve the action to update the list of the masks created
    - Update rios and fmask libs
    - Migrate and updated the documentation to github pages
    - Several minor improvements and fixes
    - Don't forget rate the plugin

    Previous versions:
    - All commits in: https://github.com/SMByC/CloudMasking/commits

tags=cloud,masking,landsat,raster,remote sensing

category=Raster
icon=icons/cloud_masking.svg
experimental=False
deprecated=False
